// If we wanna add a loader to a server side component then we can add a loading.tsx file in the same folder next to the page.tsx file
export default function Loading() {
  return (
    <div>
      <h1>Load ho rha hai</h1>
    </div>
  );
}
